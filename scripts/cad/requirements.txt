# Pinned for the CAD/mesh toolchain used by scripts/cad/*.py.
# Install into a dedicated environment, kept separate from the project venv:
#
#   python -m venv .venv-cad
#   .venv-cad\Scripts\python.exe -m pip install -r scripts/cad/requirements.txt
#
# OpenSCAD itself is a separate, non-Python dependency:
#
#   winget install OpenSCAD.OpenSCAD

trimesh>=5.0
numpy>=2.0,<2.3
scipy>=1.11
networkx>=3.0
rtree>=1.0
manifold3d>=2.0
shapely>=2.0
mapbox_earcut>=1.0
matplotlib>=3.8
